import React from 'react';
// import PropTypes from 'prop-types';
// import classnames from 'classnames';

const Test = ({name, style}) => {
    return (
        <div style={style}>{name}</div>
    )
}
export default Test;
